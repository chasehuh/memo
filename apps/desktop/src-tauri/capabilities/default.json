{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the agentnote shell (no custom IPC).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
